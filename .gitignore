# 連線設定可能含密碼，不進版控。範本見 config.example.json / .env.example
config.json
.env

# 匯入紀錄
*.log

# 待匯入的資料
data/

# PyInstaller
build/
dist/
*.spec

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# 離線安裝用的 wheel 要進版控 —— 目標機器有 proxy 407，
# 沒辦法從 PyPI 下載，只能靠 git 帶過去。約 27 MB。
